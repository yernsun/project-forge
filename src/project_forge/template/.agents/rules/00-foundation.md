# Foundation rules

- Keep generated profile boundaries intact; absent components must not become implicit dependencies.
- Services own workflows. API, workers, repositories, and UI components remain adapters.
- Make external side effects explicit and testable.
- Update documentation when an architectural boundary changes.
