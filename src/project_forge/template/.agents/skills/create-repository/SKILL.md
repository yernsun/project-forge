---
name: create-repository
description: Add or modify Psycopg repositories, persistence mappings, and safe fixed or conditional SQL in this generated project.
---

Keep SQL and row mapping in the repository. Bind named values, compose identifiers only from local
allowlists, and use a typed filter with canonical predicate ordering for conditional searches. Use
`app.db.query.SqlPredicateBuilder` for reusable safe predicate mechanics while keeping business
columns, joins, and ordering in this repository. Escape LIKE patterns, use array parameters for
variable lists, and choose preparation policy deliberately. Never commit, publish, or accept raw SQL
fragments. Add query-shape tests.
